{% extends '@EasyAdmin/page/content.html.twig' %}
{% block page_content %}
    {{ form_start(form, {'multipart': true}) }}
    <br>
    <div class="col-md-2">{{ form_row(form.uploadFile) }}</div>
    <br>
    <div class="col-md-2">
                <button type="submit" class="btn btn-primary">Импортировать</button>
    </div>
    {{ form_end(form) }}
{% endblock %}